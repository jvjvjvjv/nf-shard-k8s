apiVersion: v1
kind: Service
metadata:
  name: postgres-nfshard
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: {{ .Values.postgres.service.port }}
      name: postgres
  selector:
    app: postgres
