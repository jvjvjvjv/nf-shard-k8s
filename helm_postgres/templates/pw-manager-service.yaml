apiVersion: v1
kind: Service
metadata:
  name: pw-manager
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP 
  ports:
    - port: 5000
      targetPort: {{ .Values.pwManager.port }}
      name: http

  selector:
    app: pw-manager
